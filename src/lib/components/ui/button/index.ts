import Root, {
	buttonVariants,
	type ButtonProps,
	type ButtonVariant,
	type ButtonSize
} from './button.svelte';

export {
	Root,
	Root as Button,
	buttonVariants,
	type ButtonProps,
	type ButtonVariant,
	type ButtonSize
};
