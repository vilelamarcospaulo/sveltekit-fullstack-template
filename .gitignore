node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# wrangler-only local vars for the queue consumer Worker (see
# .dev.vars.example / wrangler.queue.jsonc) — not matched by the .env.*
# pattern above since wrangler's own convention is ".dev.vars", not ".env".
.dev.vars
!.dev.vars.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Claude Code parallel-agent worktrees (tooling-internal, not project source)
.claude/worktrees
